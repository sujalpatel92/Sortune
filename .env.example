# Copy this file to .env and fill in the values
# Do NOT commit your real .env (it is already in .gitignore)
# --- Sortune environment variables ---

# Runtime environment
SORTUNE_ENV=dev

# Redis connection (use redis service in docker compose)
REDIS_URL=redis://redis:6379/0

# If running locally without Docker, override with:
# REDIS_URL=redis://localhost:6379/0

# OpenAI API key (optional, for AI-powered playlist naming)
OPENAI_API_KEY=replace_me

# Optional Postgres (future expansion)
DATABASE_URL=postgresql+psycopg://user:pass@db:5432/sortune

# YouTube Music API (ytmusicapi OAuth client)
YT_API_CLIENT_ID=
YT_API_CLIENT_SECRET=

# Optional privacy: your personal playlist id and display name
YT_PLAYLIST_ID=
YT_PLAYLIST_NAME=

# ---------- AI / LLM ----------
# Provider selection
#   Supported: 'langchain'
SORTUNE_LLM_PROVIDER=langchain
# Backend within provider (for langchain): 'openai'
SORTUNE_LLM_BACKEND=openai
# Model identifier (for OpenAI via LangChain)
SORTUNE_LLM_MODEL=gpt-4o-mini
# Default temperature (float)
SORTUNE_LLM_TEMPERATURE=0.6
# Optional global seed for deterministic runs (if backend supports)
# SORTUNE_LLM_SEED=42
